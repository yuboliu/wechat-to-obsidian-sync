# 仓库内一律 LF。
#
# Windows 上 core.autocrlf=true 会把工作区写成 CRLF，于是不同来源的同名文件
# （本地保留版 vs 从仓库 checkout 的版本）用 diff 比会显示「整个文件都改了」，
# 实际内容一字不差 —— 2026-09-23 因此误判过两次差异范围。
# 显式声明行尾，让仓库与机器设置解耦。
* text=auto eol=lf

# 二进制（不参与行尾规范化）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
